# モデルの取り決め

C コアと Python 参照実装は、この文書の式をそのまま実装する。
両者が同じ式を実装していることが前提で、突き合わせはその実装が一致しているかを見る。

**いまは実装が確定していて、この文書が追う側。**

- **`py/ref.py` が仕様の実装**（式・符号・積分の順序はここが正）。`core/twin.c` はそれに
  突き合わせて検証する。両者は倍精度でビット一致している。
- **出力バイナリの並びは `core/FORMAT.md` が唯一の正典**（現行 `RFILL004`）。
- この文書は「何をどう扱うと決めたか」の取り決めと、その理由を残す場所。
- **食い違ったら実装の値を採り、この文書を直す。** 例外は物理として実装が誤っている場合で、
  そのときは実装を直したうえで経緯をここに残す（4.5.1 節がその例）。

すべてパラメータは `params.json` を読む。数値をコードに直書きしない。
この文書に書いてある数値は、`params.json` の値と実装の出力から取った確認用であって、
入力ではない。値が合わない場合に疑う順は、まずこの文書、次に実装、`params.json` は最後。

## 0. rev.3 で何が変わったか

機構がサーボ直結の割出しから、カム式インデックスユニット＋誘導ギヤモータに変わった。
`params.json` は rev.3 が正典で、この文書はそれに合わせて書き直したもの。
前版から**取り決めごと消えたもの**を先に挙げる。実装から削る対象がこれ。

| 消えたもの | 理由 | 代わり |
|---|---|---|
| 位置・速度・電流のカスケード | 制御系がテーブル角に関与しない | 4 節。カム形状が角度を決める |
| 台形速度プロファイルと加速区間比 | 指令が存在しない | 4 節。変形正弦カム曲線 |
| 追従誤差・整定 | 追従する対象が無い | 無し |
| バックラッシュ（空走＋歯当たり） | 停留部で予圧されるので原理的に生じない | 8 節。カムフォロワ摩耗を別に置く |
| モータ側慣性換算 `J_total = J_rotor + J_load/i^2` | 入力軸速度を規定量として扱うので不要 | 3 節・5 節 |
| ステーション単位の充填量アンバランス | 単ノズルなので特定ホルダだけ量が変わることは起きない | 8 節。欠品（ボトルが無い） |

**足したもの**は、コリオリ結合（2 節）、慣性と質量分布（3 節）、摩擦と効率（5 節）、
垂直荷重経路（6 節）、受け渡し（8 節）。

## 0.1 rev.3 を書いたあとに動いたもの（2026-07-23）

rev.3 で書き直したあと、実装（`py/ref.py` と `core/twin.c`）を通す過程で
物理と工程配置がさらに動いた。**実装が正で、この文書が追う側。**
食い違ったときは実装の値を採る（`pytest py/ -q` 67 件と `make test` が通っている状態が基準）。

| 動いたもの | 前 | いま | どこ |
|---|---|---|---|
| 排出の世界角 | 270 deg | **225 deg** | 1.2 / 8.1。星車どうしの干渉（8.1 節に幾何） |
| 滞留 | 7 割出し = 21 s | **6 割出し = 18.0 s** | 1.2 / 2.5 |
| 定常のテーブル上 | 満量 6 + 空瓶 1 + 空ホルダ 1 | **満量 5 + 空瓶 1 + 空ホルダ 2** | 3.3 |
| `J_load`（液を全量剛体） | 0.7406 kg m^2 | **0.71904 kg m^2** | 3.3 |
| `T_slosh` の符号 | 実装が足していた | **引く**（この文書 4.5 が正しかった） | 4.5 |
| テーブル軸ピークトルク | 1.43 N m | **1.4200 N m** | 4.5 |
| 定常のアンバランス | 1.6818 N m | **2.2253 N m** | 6.2 |
| 水平力の列名 | `f_tab_t` / `f_tab_r` | **`f_tab_x` / `f_tab_y`**（世界座標 x, y の順） | 10.1 |
| 在荷フラグ | 無し | **`present` を追加**（`V = 0` では空瓶と欠品が分けられない） | 10.2 |
| 水平力の符号 | 力とトルクで向きが不揃い | **`F_react` に統一**（空瓶の慣性反力も同じ向き） | 2.4 |
| 効率の掛け方 | 絶対値で常に `/eta` | **場合分け**（`T_table*om >= 0` で `/eta`、それ以外 `*eta`） | 4.7 |
| `dJ_load/dt * om` の項 | 無し | **入れた**（解析微分。公称条件では厳密に 0） | 4.5 / 7.4 |
| スロッシングの積分基底 | 「世界座標で積分」と書いていた | **回転基底**（3 項を明示） | 2.2 |
| こぼれの落とし方 | 「楔として積分する」と書いていた | **柱のまま**（保守側。既定条件では届かない） | 2.6 |
| ダンプの版 | — | **`RFILL004`**（`core/FORMAT.md` が正典） | 10 節 |

**入れないと決めたもの**（理由つき。「まだ入っていない」ではなく「入れない」）:

| 入れないもの | 理由 | どこ |
|---|---|---|
| PLC の 5 ms 格子 | 開指令 1.550 s はもともと格子点。狙った揺れは消えず、閉じ側に 1.65 mL の量子化が増えるだけ | 7.1 |
| カムフォロワ摩耗の角度オフセット | 接触剛性が `params.json` に無い。当たりはイベントで出す | 8.4 |
| 水平反力による曲げ `M_horiz` | 腕の長さが未確認。混ぜるとあとから分離できない | 6.1 |
| モータ電流 | トルク電流係数と無負荷電流が無い | 4.7 |

**捨てた数値**（もう使わない。古いメモに残っていたら消すこと）:

- `T = 0.26661 s` は誤り。正しくは **0.26657 s**（`1/3.751360`）。
- コリオリのモード分裂 `±3.3%` は旧サーボ機の値。`Omega = 0.7854 rad/s` は
  旧台形プロファイルのピーク角速度で、カム曲線ではただの通過点。**分裂は結果であって
  入力ではない**（2.3 節）。
- 「垂直荷重 / スロッシング = 38.43 倍」は旧サーボ機（定常 4 満 + 4 空）の値。
  いまの配置では **31.8 倍**（6.2 節）。

---

## 1. 座標と量

### 1.1 世界座標と回転

- 世界座標は右手系。`z` 上向き。テーブルは `z` 軸まわりに回る。
- テーブル角 `th_t` [rad] は単調増加。1 タクトで `dth = 2*pi/N = 45 deg = 0.7853982 rad` 進む。
- ステーション（ホルダ）番号 `i = 0 .. N-1`。ステーション `i` のボトル中心の世界角は

```
th_i = th_t + i*dth
p_i  = Rp * (cos th_i, sin th_i, 0)          # Rp = pitch_circle_diameter/2 = 0.225 m
```

- 局所基底。半径方向 `r_hat_i = (cos th_i, sin th_i, 0)`（外向き）、
  接線方向 `t_hat_i = (-sin th_i, cos th_i, 0) = z_hat x r_hat_i`（回転の進む向き）。

### 1.2 工程の世界角

ノズルは世界座標に固定で動かない。工程は `params.json` の `stations`。

| 工程 | 世界角 | 世界角の番号 `j` |
|---|---:|---:|
| 排出 | **225 deg** | 5 |
| 供給 | 315 deg | 7 |
| 充填 | 0 deg | 0 |

`k` 番目の停留（`k = 0, 1, 2, ...`）では `th_t = (k+1)*dth`。
世界角 `j*dth` に居るホルダ番号は

```
holder(k, j) = (j - (k+1)) mod N
```

供給 `j=7`、充填 `j=0`、排出 `j=5`。1 本のボトルは供給で入り、次の停留で充填され、
そこから 5 回の割出しを経て排出まで運ばれる。**滞留は 6 割出し = 18.0 s。**

滞留の割出し回数は工程角だけから決まる。実装はホルダ番号を固定で持たない。

```
n_res = ((discharge_deg - infeed_deg) / 45 deg) mod 8 = ((225 - 315)/45) mod 8 = 6
```

**排出を 270 deg から 225 deg へ動かした理由は 8.1 節**（星車どうしが抱えるボトルの干渉）。
排出角を動かすと `n_res` も定常のホルダ並びも動く。片方だけ直すと必ず食い違う。

### 1.3 ボトル中心が受ける加速度

テーブル角速度 `om`、角加速度 `al` に対し、ボトル中心の世界系での加速度は

```
a_tank_i = Rp * ( al * t_hat_i - om^2 * r_hat_i )
```

局所成分に分ければ、前版と同じく

```
a_t = +Rp * al          # 接線方向
a_r = -Rp * om^2        # 半径方向（遠心力の向きに液が寄るので負）
```

**この局所成分をそのまま慣性系の量として振り子に入れてはいけない**（前版の誤り）。
実装は回転基底で積分し、基底が回ることから出る 3 項（コリオリ・オイラー・遠心）を
明示的に持っている（2.2 節）。`a_t` / `a_r` はその式の右辺に入る強制項。

### 1.4 符号の約束（絵にすると必ず引っかかる）

- `phi_t > 0` = 液面が**回転の進む向き側**で高い。減速中にこうなる。
- `phi_r > 0` = 液面が**外側**で高い。回っている間はずっとこちら。
- 準静的極限では `phi = -a/g`。加速度と逆符号になる。
- 液がホルダに返す力とトルクは**反作用として符号を揃える**（2.4 節）。前版は
  力とトルクで符号の向きが逆だったので、ここで直す。

---

## 2. スロッシング（等価振り子・1 次モード・コリオリ結合込み）

### 2.1 固有振動数と等価振り子

円筒容器、内半径 `R = 0.0325 m`、液深 `h`。1 次反対称モードについて

```
eps1 = 1.8412                              # J1'(x) = 0 の第 1 根
w1^2 = (g * eps1 / R) * tanh(eps1 * h / R)
L1   = g / w1^2                            # 等価振り子の長さ
```

`h` から**毎ステップ** `w1` と `L1` を計算し直す。定数化は不可。
ただし `h/R` が大きいと `tanh` が飽和して周波数はほとんど動かない。
今回の寸法では液深 50 mm で最終値の 99.7 % に達する。効くのは注ぎ始めだけ。誇張しない。

満量（400 mL、`h = 120.544 mm`）での値:

| 量 | 値 | 出どころ |
|---|---:|---|
| `w1` | 23.5705 rad/s | 上式に `R`, `h`, `g` |
| `f1` | 3.75136 Hz | `w1/(2*pi)` |
| `T1` | 0.26657 s | `1/f1` |
| `L1` | 17.652 mm | `g/w1^2` |
| 半値半幅 `zeta*f1` | 0.018757 Hz | `zeta = 0.005` |

### 2.2 運動方程式（回転基底・これが正）

**実装は回転基底（接線 `t` / 半径 `r`）で積分する。**
前版のこの節は「世界座標で積分すること」と書いていたが、実装は回転基底を採った。
出力する量（`phi_t` / `phi_r`）が回転基底の量で、世界座標で積分すると毎ステップ
射影し直すことになり、液深 `h` が充填で変わる場合の扱いも増えるため。
**文書を実装に合わせる。** 世界座標で書きたい場合は下の等価な形を使う。

状態はステーション `i` ごとに振り子角 4 つ `(phi_t, dphi_t, phi_r, dphi_r)`。
`zeta` は `liquid.slosh_damping_ratio = 0.005`、`L1 = g/w1^2`。

```
phi_r'' =  2*om*phi_t' + al*phi_t + om^2*phi_r - 2*zeta*w1*phi_r' - w1^2*phi_r - a_r/L1
phi_t'' = -2*om*phi_r' - al*phi_r + om^2*phi_t - 2*zeta*w1*phi_t' - w1^2*phi_t - a_t/L1
```

`2*om*phi'` がコリオリ、`al*phi` がオイラー、`om^2*phi` が遠心。**3 項とも落とさない。**
`a_t`, `a_r` は 1.3 節の局所成分。合成傾きは `tilt = sqrt(phi_t^2 + phi_r^2)`。

世界座標で書くなら、水平変位 `u_i = (u_x, u_y)` について

```
u_i'' + 2*zeta*w1*u_i' + w1^2 * u_i = - a_tank_i          # 成分ごとに 2 本
phi_t = (u_i . t_hat_i) / L1 ,  phi_r = (u_i . r_hat_i) / L1
```

となり、上の 3 項は基底の回転そのものから出る。2 つは等価。
**実装は回転基底のほうを既定とする。**

**取り決め（`params.json` に無いのでここで決めた）**: 減衰 `2*zeta*w1*phi'` は
**容器に対する相対速度**（回転基底での微分）に掛ける。減衰の実体が壁の境界層なので
相対のほうが素直で、この選び方だと 2.4 節の `a1` の式が近似なしで閉じる。
慣性系の微分に掛ける流儀との差は `2*zeta*om/w1 = 4e-4` 相当で、ピーク傾きの差は
7e-5 mrad。数値としては見えないが、**どちらを採ったかで `a1` の式が変わる**ので明記する。

### 2.3 モード分裂（コリオリの効き方）

コリオリ結合は 1 次モードを共回転・逆回転の 2 本に `±om` だけ割る。
**これは入力ではなく結果で、2.2 節の式を回せば自然に出る。実装で分裂幅を直接与えてはいけない。**

分裂は解析的に出る。`z = phi_r + i*phi_t` と置くと（減衰なし・`om` 一定）

```
z'' + 2i*om*z' + (w1^2 - om^2) z = 0   ->   lambda = w1 - om , -(w1 + om)
```

つまり `w1 ± om` の 2 本。幅は `w1` に依らず `om` だけで決まる。

**`om` は割出しの中で 0 から最大まで動くので、分裂幅も時間で動く。
代表値を 1 つに決めないこと。** 半値半幅は `zeta*f1 = 0.018757 Hz`。

| `om` の取り方 | `om` [rad/s] | `om/w1` | 分裂 `om/(2*pi)` [Hz] | 半値半幅の何倍か |
|---|---:|---:|---:|---:|
| 停留中 | 0 | 0 % | 0 | 0 |
| タクト平均（停留込み） | 0.26180 | 1.11 % | 0.0417 | 2.2 |
| 割出し中の実効値（`om` の二乗平均平方根） | 0.61142 | 2.59 % | 0.0973 | 5.2 |
| 割出し中の最大 | 0.92133 | 3.91 % | 0.1466 | 7.8 |

**前版に書いていた「代表値 `±3.3 %`（`om = 0.779`）」は消した。**
3.3 % は旧サーボ機の台形プロファイルのピーク角速度 0.7854 rad/s に当たる値で、
カム曲線ではその値を通過するだけの点でしかない。**分裂は結果であって入力ではない。
実装のどこにもハードコードしないこと。**

停留中を除けば**分裂は半値幅より大きい**ので、スペクトル上で 1 本が 2 本に割れて見える。
これは「揺れの線が回転に同期しない」という前版の説明が成り立たないことも意味する。
正しくは「回転速度に応じて動く」。次数比分析で回転成分と分けるときは、この移動を織り込む。
1 タクトぶんの平均スペクトルを取ると、分裂した 2 本は停留中の 1 本のまわりに
裾を広げた形になる。**分裂幅を固定の 2 本として当てはめてはいけない。**

コリオリを入れた場合と落とした場合の差は、この機械の条件では**残留振幅で 8 % 程度**
（35.30 mrad と 38.45 mrad。2.6 節）。割出し中のピークは強制応答が支配的なので
差は 5e-5 rad しかない。前版（タクト 2.4 s）では偶然共振に乗っていたので 1.8 倍の差が
出ていたが、タクト 3.0 s では共振から外れているので差は小さい。**それでも入れる。**
入れない理由が「差が小さいから」では、共振に近づいたときに破綻するため。

### 2.4 反力（液がテーブルに返す力とトルク）

これが無いとセンサが何も拾えない。等価振り子の質量分割は

```
m_liq = rho * V_i
m1 = m_liq * 2*R*tanh(eps1*h/R) / ( eps1*(eps1^2 - 1)*h )    # 揺れる分。eps1 = 1.8412
m0 = m_liq - m1                                              # 容器と一緒に動く分
```

`eps1*(eps1^2-1) = 4.40045` なので、`m1/m_liq = (R/(2.2002*h))*tanh(eps1*h/R)` と同じ。
**実装は上の形をそのまま持つ**（丸めた 2.2 を定数にしない）。
満量では `m1 = 0.04901 kg`（**12.25 %**）、`m0 = 0.35099 kg`。

揺れる質量の水平絶対加速度は、運動方程式を代入して `a_tank` を消すと

```
a1 = a_tank + L1 * phi'' = -( g*phi + 2*zeta*w1*L1*phi' )
```

（`L1*w1^2 = g` を使う。成分ごとに同じ形で書ける。）
**この形が近似なしで閉じるのは、減衰を容器に対する相対速度に掛けているから**（2.2 節）。
慣性系の微分に掛ける流儀を採ると、この式に回転ぶんの補正が付く。

**符号の約束をここで固定する。** `params.json` にも前版にも書いていなかったので、
実装ごとにばらついていた。**すべて反作用側（テーブルが受ける側）に統一する。**

```
F_hold_i  = m0*a_tank_i + m1*a1_i        # ホルダが中身に及ぼす力（液を動かすのに要る力）
F_react_i = -F_hold_i                    # 中身がホルダに返す力。これを出力する
```

**出力する水平力は `F_react` のほう**（前版と符号が逆になる）。
**空瓶の慣性反力も同じ向きに揃える。** ホルダ 1 個ぶんの局所成分は

```
loc_t = F_react_t - m_empty*a_t
loc_r = F_react_r - m_empty*a_r
```

で、`-m_empty*a` と反作用の向きが揃う。これを世界座標へ回して全ステーションで足したものが
`f_tab_x` / `f_tab_y`（10.1 節）。**回転基底のまま足してはいけない**（8 本が同じ状態なら
向きが打ち消し合って 0 になるのが正しく、局所量の単純和は 0 にならない）。

```
f_tab_x = sum_i( loc_r_i*cos(th_i) - loc_t_i*sin(th_i) )
f_tab_y = sum_i( loc_r_i*sin(th_i) + loc_t_i*cos(th_i) )
```

テーブル軸に返るトルクも同じ向きに揃える。

```
T_slosh = -Rp * sum_i( m1_i * a1_t_i )    # 揺れる分だけ。z 軸まわり
```

`m0` の分は 3 節の `J_load` に剛体として入っているので、ここで足すと二重計上になる。
準静的極限では `T_slosh = -m1*Rp^2*al` となり、慣性としてちょうど効く（4.5 節で検算する）。
`F_react` と `T_slosh` はこの約束で同じ向き（どちらもテーブルが受ける側）になる。

**液が入っていないホルダ（`V_i = 0`）は、空瓶の慣性反力も数えていない。**
実装が `V <= 0` のホルダを丸ごと飛ばしているのに合わせた。空瓶 1 本ぶん
（`0.025 kg * 0.43416 m/s^2 = 0.011 N`）が抜ける。既知の食い違いとして 11 節に挙げる。

半径方向の力は `z` 軸まわりのトルクには効かないが、**支持部の曲げと架台の振動には効く**ので、
世界座標の合力ベクトルとして残す（6 節）。

### 2.5 液は容器と一緒に回っていない

コリオリ項を入れる根拠がこれ。液が容器と剛体回転しているなら回転項は消える。
スピンアップの目安時間 `h/sqrt(nu*Omega)` は、割出し中の最大角速度で 125.6 s、
タクト平均で 235.6 s。**1 本のボトルの滞留は 6 割出し = 18.0 s** で、7 倍から 13 倍短い。
粘性拡散の厚み `sqrt(nu*t)` も 18 s で 4.24 mm（`R` の 13 %）にしかならない。
つまり液はほぼ非回転のままで、軸対称容器の自転は理想流体のモードに効かない。
ボトルがホルダで自転するのか置いてあるだけなのかは、この結論を変えない。

### 2.6 液面の傾き・こぼれ・適用範囲

壁での液面の上下動:

```
dz = R * tan(tilt)     # tilt が 1 rad を超えるところでは 1 rad で頭打ちにする
```

こぼれ判定は `h + dz > body_height` になったら超過分を積算し、`h` から差し引く。

```
over    = h + dz - body_height
spilled = over * pi*R^2                  # 実装はこの「柱」で落とす
```

**実装は柱のまま。楔にしない。** 実際に縁を越えるのは傾いた液面が縁の平面を切る
楔状の部分だけで（漸近形は `(8/15)*sqrt(2R)*over^(5/2)/tan(tilt)^(3/2)`、
`py/NOTES.md` 6 節）、柱で落とすと **1〜2 桁の過大評価**になる。
それでも柱を採ったのは 2 つの理由による。

- **保守側**（多めに見積もる）で、こぼれたか否かの判定（`over > 0`）は変わらない。
- 既定条件では**液面が縁に届かない**ので、この差が結果に一切出てこない。
  頭上空間は 29.5 mm、既定条件のピーク傾き 93.9 mrad での `dz` は 3.05 mm。

**したがって「何 mL こぼれたか」を数字として主張しないこと。** 出しているのは上限値。
楔に差し替えるなら C と Python の両方を同時に直す（片方だけ直すと突き合わせが壊れる）。

**こぼれ量は主要な指標にしない。** 頭上空間 29.5 mm を越えるには傾き 42 deg が必要で、
それは適用範囲を大きく外れる。こぼれは「線形近似が破れた領域に入った」目印としてだけ使う。

**適用範囲**: 壁面の液面上下 `dz/R` が 0.1（傾き約 5.7 deg）までは減衰が線形。
0.2（約 11.3 deg）を超えると波が砕けたり回り始めて、この式は成り立たない。
毎ステップ最大傾きを記録し、0.2 を超えたらラッチして出力に残す。

**確認用の数値**（1 本のボトルを満量固定、静止から 1 回だけ割出し、`zeta = 0.005`、
`dt = 2.5e-5`、半陰的オイラー。充填・排出・欠品は無し）:

| 量 | 値 | 条件 |
|---|---:|---|
| 割出し中のピーク傾き | 68.07 mrad | `dz/R = 0.0682`。コリオリの有無で 5e-5 rad しか変わらない |
| 停留に入ってからの残留振幅 | 35.30 mrad | 2.2 節どおり（コリオリ入り） |
| 同上・コリオリを落とした場合 | 38.45 mrad | 参考。差は 8 %（1.089 倍） |

ピーク 68.07 mrad は準静的な傾き `a_t_max/g = 44.27 mrad` の 1.54 倍。
残留の 35 mrad は、**充填が始まる時点でまだ揺れているという意味**で、絵の主役になる。

**通しで回した場合**（工程配置つき・供給と排出と充填を入れた 12 タクト、`dt = 2.5e-5`）:

| 量 | 値 |
|---|---:|
| 合成傾きの最大 | 93.94 mrad（割出し中） |
| 停留中の最大 | 47.86 mrad |
| `dz/R` の最大 | 0.09422（限界 0.20 の内側） |
| こぼれ | 0 mL |

1 回だけの割出し（68.07 mrad）より大きいのは、前のサイクルの残りが乗るため。
**適用範囲の判定にはこちらを使う。**

---

## 3. 慣性と質量分布

### 3.1 テーブル板

回転軸まわりの**極慣性**を使う。

```
m_plate = rho_plate * pi * r_plate^2 * t_plate = 7900 * pi * 0.28^2 * 0.008 = 15.566 kg
J_bare  = (1/2) * m_plate * r_plate^2 = 0.5 * 15.566 * 0.28^2 = 0.6102 kg m^2
```

**前版の 0.45 は `(1/4) m r^2`（直径まわり）を使っていた係数の誤り。**
回転軸まわりは `1/2`。板厚も t12 から t8 に落ちている。

### 3.2 ボトルの中身で変わる分

```
J_load(t) = J_bare + sum_i( m_i(t) * Rp^2 )
```

`m_i` はステーション `i` に載っている質量。**液は `m0` だけを数える**（`m1` の分は
2.4 節の `T_slosh` として別に効くので、両方入れると二重計上）。

```
m_i = m_empty + m0_i          # ボトルがある場合
m_i = 0                       # ホルダが空（ボトルそのものが無い）場合
```

### 3.3 定常でどうなるか

1.2 節の工程配置（供給 315 / 充填 0 / **排出 225** deg、滞留 6 割出し）から、
割出し中のテーブル上は次のようになる。**数え方は工程角から一意に決まる。**

ホルダ `i` の齢 `k = ((i*45deg - infeed_deg)/45deg) mod 8` に対し

| `k` | 状態 | 個数 | 世界角（テーブル角 0 のとき） | 1 個の質量 |
|---|---|---:|---|---|
| `0 < k < n_res` | 満量ボトル（充填済み、排出待ち） | **5** | 0 / 45 / 90 / 135 / 180 deg | 0.425 kg（液 0.400 + 瓶 0.025） |
| `k = 0` | 空ボトル（供給されたばかり、充填前） | **1** | 315 deg | 0.025 kg |
| `k >= n_res` | ホルダのみ（排出済み、供給前） | **2** | 225 / 270 deg | 0 |

```
sum m_i = 5*0.425 + 0.025 + 0 = 2.150 kg
J_load  = 0.6102 + 2.150   * 0.225^2 = 0.71904 kg m^2    # 液を全量剛体として数えた場合
J_load  = 0.6102 + 1.90493 * 0.225^2 = 0.70664 kg m^2    # m0 だけ数えた場合（実装のログはこちら）
```

**2 つの `J_load` は別の量で、用途が違う。混ぜないこと。**

| どちらか | 値 | 何に使うか |
|---|---:|---|
| 液を全量剛体 | 0.71904 kg m^2 | 剛体換算のピークトルク 1.38748 N m（4.5 節の検算・選定） |
| `m0` だけ | 0.70664 kg m^2 | 運動方程式。`m1` は `T_slosh` として別に入る（二重計上を避ける） |

差は 1.7 % で、これがちょうど揺動質量 `m1` の合計 `sum(m1)*Rp^2 = 0.01240 kg m^2` にあたる。

`params.json` の `_note` にある **「7 本満量＋1 本空」= 3.000 kg = 0.7621 kg m^2 は
全ホルダにボトルが載っている前提の上限値**で、いまの工程配置とは 6.0 % 合わない。
**選定の余裕を見るには 0.7621 を使い、運動方程式には毎ステップ計算した実際の `J_load` を
使う。** 実装は定数を持たない。

前版の「6 本満量＋1 本空瓶＋1 本空ホルダ（0.7406）」は排出を 270 deg に置いていたときの
数え方（滞留 7 割出し）で、排出が 225 deg へ動いた時点で成立しない。
その前の「4 本満量・4 本空」は排出と供給を同じ角に置いていたときの数え方で、
これも成立しない。**排出角を動かしたらこの節も一緒に動く。**

---

## 4. 割出し軸（カム式インデックスユニット）

### 4.1 独立変数は入力軸角

**位置ループも速度ループも電流ループも存在しない。位置を決めるのはカムの形状だけ。**
テーブル角は時間の関数ではなく、カム入力軸角 `psi` の関数。

```
psi(t) = Om_cam * t ,   Om_cam = 2*pi / tact = 2.0944 rad/s = 20 rpm （一定・連続回転）
```

`0 <= psi < pi` が割出し、`pi <= psi < 2*pi` が停留。割付 180 deg / 停留 180 deg なので
`index_time = dwell_time = tact/2 = 1.5 s`。

**入力軸は 1 サイクルごとに起動停止しない。回りっぱなし。** ここが前版と決定的に違う。

### 4.2 変形正弦カム曲線

無次元時間 `tau = psi/pi`（割出し区間で 0 から 1）に対し、無次元の位置 `S`、速度 `Vc`、加速度 `Ac`
を次で定める。`S(0)=0`, `S(1)=1`, `Vc(0)=Vc(1)=0`, `Ac(0)=Ac(1)=0`。

```
Ca = 4*pi^2/(pi+4) = 5.527957        # 無次元最大加速度
Cv = Ca/pi         = 1.759603        # 無次元最大速度
```

加速度:

```
0     <= tau < 1/8 :  Ac = Ca * sin(4*pi*tau)
1/8   <= tau < 7/8 :  Ac = Ca * cos( (4*pi/3) * (tau - 1/8) )
7/8   <= tau <= 1  :  Ac = Ca * sin(4*pi*(tau - 1))
```

速度（`Ac` の積分。区間の継ぎ目で連続）:

```
0     <= tau < 1/8 :  Vc = (Ca/(4*pi)) * (1 - cos(4*pi*tau))
1/8   <= tau < 7/8 :  Vc = Ca/(4*pi) + (3*Ca/(4*pi)) * sin( (4*pi/3)*(tau - 1/8) )
7/8   <= tau <= 1  :  Vc = Ca/(4*pi) - (Ca/(4*pi)) * cos(4*pi*(tau - 1))
```

位置（`Vc` の積分）:

```
S18 = (Ca/(4*pi)) * (1/8 - 1/(4*pi))
S78 = S18 + (Ca/(4*pi))*(3/4) + (9*Ca/(16*pi^2))*2

0   <= tau < 1/8 :  S = (Ca/(4*pi)) * ( tau - sin(4*pi*tau)/(4*pi) )
1/8 <= tau < 7/8 :  S = S18 + (Ca/(4*pi))*(tau - 1/8)
                         + (9*Ca/(16*pi^2)) * ( 1 - cos( (4*pi/3)*(tau - 1/8) ) )
7/8 <= tau <= 1  :  S = S78 + (Ca/(4*pi))*(tau - 7/8)
                         - (Ca/(16*pi^2)) * ( sin(4*pi*(tau - 1)) + 1 )
```

実装の自己検査に使えること: `S(1) = 1`（丸め誤差の範囲）、`max Vc = Cv`（`tau = 1/2`）、
`max Ac = Ca`（`tau = 1/8`）、`Vc` と `Ac` は区間の継ぎ目で連続。
**ジャークが飛ぶのは `tau = 0` と `tau = 1` の 2 箇所だけ**で、継ぎ目 1/8・7/8 では連続。
これが台形（4 箇所で加速度が階段状に飛ぶ）との違いで、高調波の落ちが速い。

### 4.3 テーブル角・角速度・角加速度

```
割出し中（0 <= psi < pi、tau = psi/pi、cyc = floor(t/tact)）:
  th_t = dth * ( cyc + S(tau) )
  om   = dth * Vc(tau) / T_index
  al   = dth * Ac(tau) / T_index^2

停留中（pi <= psi < 2*pi）:
  th_t = dth * (cyc + 1) ,  om = 0 ,  al = 0
```

**`th_t` は積分しない。** カムは剛体拘束として扱う（4.6 節）。

| 量 | 式 | 値 |
|---|---|---:|
| 最大角速度 | `dth*Cv/T_index` | 0.92133 rad/s |
| 最大角加速度 | `dth*Ca/T_index^2` | 1.92962 rad/s^2 |
| 最大接線加速度 | `Rp*al_max` | 0.43416 m/s^2（0.04427 g = 44.3 mg） |
| 平均テーブル速度 | `dth/tact` | 0.26180 rad/s（2.5 rpm） |

`params.json` の 1.92964 / 0.43417 は丸めた `Ca = 5.528` から出した値。
実装は閉じた形の `Ca = 4*pi^2/(pi+4) = 5.527957` を使うので 1.92962 / 0.43416 になる。
**相対差 1e-5。実装の値を採る。**

接線加速度 0.04427 g は、満量ボトルの転倒しきい値（重心高さと接地半径の比から出る値。
`params.json` の `_kinematics_note` にある 0.532 g）に対して 12.0 倍の余裕がある。
ただし**重心高さは `params.json` に無い**ので、この 12 倍は追いかけられていない（11 節）。

### 4.4 バックラッシュは無い

カムフォロワは停留部でカムリブに予圧されるため、原理的にバックラッシュが無い。
`indexer.backlash_deg = 0.0`。**前版のバックラッシュ故障モード（空走＋非弾性衝突）は
この機構では成立しないので、実装ごと削る。** 代わりに置くのはカムフォロワ摩耗（8.4 節）。

### 4.5 トルクは逆算する

角度が決まっているので、トルクは運動から逆に求める。テーブル軸まわり:

```
T_table = J_load*al + (dJ_load/dt)*om + c_visc*om - T_slosh + T_bear
```

`J_load` は **`m0` だけを数えた側**（3.3 節。定常で 0.70664 kg m^2）。

| 項 | 意味 | 値の目安 |
|---|---|---|
| `J_load*al` | 主項 | ピークの瞬間で 1.36353 N m |
| `(dJ_load/dt)*om` | 充填中に質量が増える分（7.4 節） | 公称条件では厳密に 0（停留中に充填が終わるため） |
| `c_visc*om` | テーブル粘性。`indexer.table_viscous_Nms_rad = 0.05` | ピークの瞬間で 0.01147 N m（`c_visc*om` の最大は 0.04607） |
| `-T_slosh` | 2.4 節。テーブルが受けるトルクなので**引く** | ピークの瞬間で +0.04505 N m |
| `T_bear` | 旋回軸受の摩擦 | `params.json` に無いので 0（11 節） |

### 4.5.1 `T_slosh` の符号 — 決着とその検算

**この文書（前版の 4.5 節）が正しく、`py/ref.py` が誤っていた。**
参照実装は `T = J*al + c*om + T_slosh` と**足して**いた。C 実装の担当が突き合わせの過程で
見つけ、参照実装を直した。**同じ誤りが二度入らないように経緯と検算をここに残す。**

検算は準静的極限で行う。液が容器に完全に追従する（`a1_t = Rp*al`）極限では

```
T_slosh = -Rp * sum(m1 * a1_t) = -sum(m1) * Rp^2 * al        （負）
-T_slosh = +sum(m1) * Rp^2 * al                              （慣性として素直に足される）

T_table = J_load(m0 だけ)*al - T_slosh
        = ( J_rigid + sum(m1)*Rp^2 ) * al
        = J_load(液を全量剛体) * al                          -> 剛体換算に一致
```

**引く向きだと剛体換算にちょうど戻り、足す向きだと揺動質量ぶんが 2 回引かれて
剛体換算より小さくなる。** 後者は物理として成立しない。
テーブル軸ピークトルクで 1.3428 -> 1.4200 N m（5.8 %）動く。
`py/test_ref.py::test_slosh_torque_is_subtracted_not_added` が番人として入っている
（符号を逆にしたら落ちる）。

### 4.5.2 ピークの内訳

定常（2 タクト目以降）のテーブル軸ピークは **1.4200 N m**。

ピークの瞬間（`psi = 0.39165 rad = 22.44 deg`、`dt = 0.2 ms`・1 kHz ログで拾った峰の点）

| 項 | 計算 | 値 [N m] |
|---|---|---:|
| `J_load*al` | 0.70664 × 1.929604 | 1.36353 |
| `c_visc*om` | 0.05 × 0.229367 | 0.01147 |
| `-T_slosh` | ― | 0.04505 |
| 合計 | ― | **1.42004** |

剛体換算 `0.71904 * 1.92962 = 1.38748 N m` の **1.0235 倍**。

`J` から抜いた `m1` ぶん（`sum(m1)*Rp^2*al = 0.245074 * 0.050625 * 1.929604 = 0.02394 N m`）が
`-T_slosh` として 0.04505 で戻る。1.9 倍になっているのは振り子が行き過ぎるぶんで、
準静的なら同じ値に戻る。**この「行き過ぎ」が動的な項であることの証拠**で、
慣性の数え方の誤りではない。

峰は平らなので、**5 桁目はログ格子の取り方で動く**（`dt = 2.5e-5` で全ステップ拾うと
`psi = 0.39197` で 1.42009）。**桁として 1.4200 N m**、剛体換算比 1.0235 倍まで主張する。
（`py/test_ref.py` の docstring は 1.42007 と書いているが、同じ条件で実際に出るのは
1.42004。`-T_slosh` を 0.04507 と書いているぶんのずれで、試験の判定幅の内側。）

`params.json` の慣性上限 0.7621 を使うと `J_load*al` のピークは 1.4705 N m。
**この 1.4705 は選定側の上限で、実装が出す値ではない。**

### 4.6 剛体拘束として扱うこと・そのとき何を捨てたか

カム機構・減速機・カップリング・支持軸のねじり剛性は**無限大として扱う**。
したがって `th_t` は 4.3 節の式そのもので、駆動系のねじり共振はモデルに現れない。

捨てているのは次の 2 つ。記事では断る。

- ねじり共振とその励振（実機なら数十 Hz から 100 Hz 台に線が立つ）。
- 入力軸速度の変動。誘導モータはすべりで回るので、負荷トルクが変われば
  入力軸速度もわずかに動く。ここでは `Om_cam` を一定とした（4.7 節）。

### 4.7 駆動（誘導ギヤモータ＋インバータ）

```
入力軸トルク  T_in  = T_cam + T_drag
  T_cam = T_table * r(psi) / eta       （T_table*om >= 0 のとき。カムが負荷を駆動する側）
  T_cam = T_table * r(psi) * eta       （T_table*om <  0 のとき。負荷がカムを回す側）
  r(psi) = d th_t / d psi = (dth/pi) * Vc(tau)          # 常に 0 以上。最大 0.43990
  T_drag = indexer.input_drag_torque_Nm = 0.30 N m      # 常に回転を妨げる向き（正）
モータ軸トルク T_m = T_in / gear_ratio                   # gear_ratio = 75
```

`eta = indexer.efficiency = 0.85`。

**効率は場合分けする。絶対値で数えないこと。** `r(psi)` は常に 0 以上なので、
向きを決めるのは `T_table*om` の符号だけ。前の実装は絶対値を取って常に `/eta` にしていて、
**減速側を `1/0.85^2 = 1.38 倍`過大**に見ていた。この場合分けを入れたので
`T_in` は**符号付き**になり、減速中は引きずり 0.30 N m を下回りうる。
**「およそ」で済ませず、上の 2 場合分けをそのまま実装する。**

下の表は 3.3 節の定常（`J_load = 0.71904`、液を全量剛体）を使って、
割出し 1 回を `psi` で走査して出したもの。**スロッシング反力は入れていない**
（慣性と粘性だけの走査）。

| 量 | 値 | 出どころ |
|---|---:|---|
| `r` の最大 | 0.43990 | `(dth/pi)*Cv`（`tau = 1/2`） |
| `T_in` のピーク（慣性負荷だけ） | 0.40290 N m | `psi = 51.7 deg`。粘性・引きずりなし |
| `T_cam` のピーク（粘性込み） | 0.41548 N m | `psi = 52.4 deg` |
| `T_in` のピーク（引きずり込み） | 0.71548 N m | 上 ＋ `T_drag = 0.30` |
| `T_m` のピーク | 0.009540 N m | `T_in/75` |
| モータ定格トルク | 0.26899 N m | `40 W / (1420 rpm)` |
| 負荷率 | 3.5 % | `T_m` ピーク / 定格 |
| テーブル軸のピーク機械出力 | 0.7397 W | `max(T_table*om)`（慣性＋粘性の走査） |

**通しで回すと `T_in` は 0.015 〜 0.723 N m の範囲を動く**（12 タクト・故障なし）。
上の走査の 0.71548 より 0.008 大きいのは、通しにはスロッシング反力（`-T_slosh`）が
入っているため。ピーク機械出力も通しでは 0.754 W。**走査値と通し実測値は別物**なので、
どちらの数字か必ず添える。

`params.json` の「入力軸のピーク負荷トルク 0.76 N m」は、`T_table` の最大と `r` の最大を
**同じ瞬間として掛けた値**。この 2 つは同時に起きない（`al` が最大なのは `tau = 1/8` の
`psi = 22.5 deg`、`om` が最大なのは `psi = 90 deg`）ので、掛け合わせると 1.78 倍の過大になる。
実際のピークは 0.40290 N m（慣性負荷だけ）。
`params.json` の「ピーク機械出力 1.36 W」も同じ理由で、慣性負荷だけなら 0.7173 W。

**`T_drag = 0.30` は入力軸ピーク 0.71548 の 42 %。** 仮置きの値がこれだけ効く。
したがって**入力軸トルクを主張の根拠にしない**（5 節・11 節）。

減速の効率は `params.json` に無いので、ここでは 1.0 として扱う（11 節）。

**電流は出せない。** インバータ出力電流を作るには無負荷（励磁）電流とトルク電流係数の 2 つが
要るが、どちらも `params.json` に無い。**コアは `T_in` までを出す。**
`T_m = T_in / gear_ratio` で作れるので `T_m` も列には持たない（10.1 節）。
電流は列としても存在しない。**旧モデルではモータ電流が主要な観測量だったが、
いまは根拠が無いので出さない**（11 節）。`params.json` の `sensors.current` ブロックは
前版の持ち越しで、いまの出力とはつながっていない。

### 4.8 タクト 3.0 s で偶然共振から抜けたこと

前版（タクト 2.4 s、台形プロファイル）では、

```
タクト周波数の 9 倍高調波 = 9 / 2.4 = 3.75000 Hz
スロッシング固有振動数     =           3.75136 Hz
離調 0.00136 Hz = 半値半幅 0.018757 Hz の 7 %
```

つまり**ほぼ真上**。`Q = 1/(2*zeta) = 100` の鋭い共振に載っていたので、
基準にしていた最大傾きの 4 割はこの偶然の共振で作られていた。
「割出し時間を延ばせば揺れが減る」という説明も、この条件では成り立っていなかった。

タクト 3.0 s では

```
最寄りの高調波 = 11 / 3.0 = 3.66667 Hz
離調 0.08469 Hz = 半値半幅の 4.5 倍
```

**動作点が偶然で決まっていた状態から抜けた。**
加えて変形正弦は台形よりジャーク段差が少ない（4.2 節）ので、高調波そのものが小さい。

この 2 つは狙って設計したものではなく、機構を替えた副産物である。記事でもそう書く。
**タクトを振る解析をするときは、必ずこの離調を一緒に出すこと。**
離調が半値幅の 1 倍を切る条件では、結果はスロッシングの共振で決まってしまい、
機構の比較にならない。

---

## 5. 摩擦と効率

`params.json` の `indexer` にある 3 つ。**3 つとも出所が無い仮置き。**

| キー | 値 | どこに効くか |
|---|---:|---|
| `efficiency` | 0.85 | 4.7 節。`T_cam` の 18 %（`1/0.85 - 1`）。減速側では逆に 15 % 減らす |
| `input_drag_torque_Nm` | 0.30 | 4.7 節。**入力軸ピーク 0.71548 の 42 %** |
| `table_viscous_Nms_rad` | 0.05 | 4.5 節。`T_table` ピークの 0.8 %（ピークの瞬間で 0.01147 / 1.42004） |

`_friction_grade` は `assumed`。カタログの許容入力トルク表を当てるまでは確定しない。
**この 3 つを動かすと入力軸トルクは 3 割から 4 割動く。** したがって、

- **入力軸トルクを主張の根拠にしない。** 引きずりだけで 42 % を占める。
- **モータ電流はそもそも出さない**（4.7 節）。トルク電流係数と無負荷電流が無い。
- 感度として、3 つを ±50 % 振ったときの幅を必ず併記する。
- 記事では「仮定」と明記する。

粘性はテーブル軸トルクへの寄与が 1 % 未満なので、**テーブル軸トルクのほうは
この 3 つにほとんど依らない**。主張に使えるのはこちら側。

旋回軸受の摩擦トルクは `params.json` に無いので 0 としている（11 節）。
実機では定速区間でもトルクは 0 にならないので、**モデルの定速区間が厳密に
`T_table = c_visc*om` になるのは、この欠落のため**であることを覚えておく。

---

## 6. 垂直荷重経路

前版が丸ごと落としていた経路。**支持部の曲げの主成分はここ。**

### 6.1 曲げモーメント

ボトルの重量はピッチ円半径 `Rp` の位置に載るので、支持部に曲げモーメントを作る。
支持点を原点として、モーメントベクトル（水平方向）は

```
M_weight = g * Rp * sum_i( m_i * t_hat_i )
```

`t_hat_i = z_hat x r_hat_i` なので、`M_weight` はオフセット方向と直交する水平ベクトルになる。
`m_i` は 3.2 節と同じ（**ここでは液は全量。`m0`/`m1` の分割は水平の慣性力の話で、
重量には関係しない**）。

充填中は噴流の鉛直運動量も同じ形で入る（7.3 節）。**出力する曲げはこの 2 つだけ。**

```
M_x = Rp * sum_i( F_i * cos(th_i) )        F_i = m_i*g（鉛直下向き）＋ 充填中はジェット
M_y = Rp * sum_i( F_i * sin(th_i) )
M   = hypot(M_x, M_y) = |M_weight + M_jet|
```

`M_x` / `M_y` は「**どちらへ偏っているか**」を世界座標で持つための成分で、
曲げの中立軸はこのベクトルに直交する。物理としてのモーメントベクトルはこれを 90 deg
回したもの。ひずみへの変換は `SENSORS.md` の担当で、ここではしない。

**水平力による曲げ `M_horiz` は入れていない。**

```
M_horiz = L_arm * ( z_hat x F_tab )        # 実装に入っていない
```

`F_tab` は全ステーションの水平反力を世界座標で足したもの（2.4 節）。
`L_arm` に当てられる値は `sensors.strain.arm_length_mm = 150 mm` しかなく、
**これはセンサ側の値で、機械側の重心高さとして確かめたものではない。**
テーブル板から支持部までの距離も `params.json` に無い。
腕の長さが仮置きのまま `M` に混ぜると、あとから分離できなくなる。
**だから列には入れず、比較（6.2 節）でだけ仮の腕を掛けて桁を見る。**

### 6.2 大きさの目安

| 経路 | モーメント | 出どころ |
|---|---:|---|
| 満量 1 本ぶんの液の重量 | 0.88260 N m | `0.400*9.80665*0.225` |
| 満量 1 本ぶん（瓶ごと） | 0.93776 N m | `0.425*9.80665*0.225` |
| **定常のアンバランス正味** | **2.2253 N m** | 下記 |
| 通しで拾った `M` の最大 | 2.3795 N m | 充填中（ジェット＋充填途中の偏り） |
| 水平反力の大きさの最大 × 仮の腕 0.150 m | 0.0748 N m | `0.49898 N * 0.150 m` |
| 噴流の鉛直運動量 × `Rp`（充填中） | 0.15917 N m | 7.3 節 |

定常のアンバランスは、3.3 節の質量分布から。8 方位の単位ベクトルの和は 0 なので、
全ホルダ満量からの差だけが残る。**欠けているのは 3 か所**で、空ホルダ 2 つ（225 / 270 deg）が
ボトルごと 0.425 kg、空瓶 1 本（315 deg）が液 0.400 kg。

```
ベクトル和 = 0.425*(cos225, sin225) + 0.425*(cos270, sin270) + 0.400*(cos315, sin315)
           = (-0.01768, -1.00836)                    -> 1.00852 kg 相当 @ Rp
M          = 1.00852 * 9.80665 * 0.225 = 2.2253 N m
```

**前版の 1.6818 N m は排出 270 deg（空ホルダ 1 つ）の配置での値。** 排出が 225 deg へ
動いて空ホルダが 2 つになったので、この節も 1.32 倍になった。

**この 2.23 N m は水平反力経路の 30 倍以上**で、テーブル角に同期して回る。
つまり支持部のひずみの正常波形は、センサ雑音ではなく機械の動作で決まる。
`M` は 0 に落ちない（最小 2.2249 N m）。
欠品（8.3 節）はここに直接乗るので、**検出はこの経路が主役になるはず**。そこを確認したい。

### 6.2.1 「垂直荷重は水平の何倍か」の数え方

**31.8 倍。** ただし数え方に条件が 3 つ付く。

```
2.3795 N m / ( 0.49898 N * 0.150 m ) = 31.8
```

1. **腕 `L_arm = 0.150 m` は仮置き。** `sensors.strain.arm_length_mm` を流用しただけで、
   **機械の重心高さとして確かめていない（未確認）。** この比は腕に反比例するので、
   腕が倍なら 16 倍になる。**桁の話としてしか読めない。**
2. **2 つとも別々の瞬間の最大値どうしの比。** `M` の最大は充填中、水平合力の最大は
   割出しの加速ピークで、同時には起きない。
3. **旧サーボ機の「38.43 倍」は使わない。** 定常が「満量 4 本 + 空 4 本」だった頃の値で、
   いまの工程配置には当てはまらない。

**`M_bend` と `T_slosh` を直接比べないこと。** どちらも単位は N m だが、
**`M_bend` は水平軸まわりの曲げ、`T_slosh` は z 軸まわりのねじり**で、軸が違う。
比を取ると 51.6 倍（`2.3795 / 0.04611`。`T_slosh` の最大を拾う刻み次第で 52.0 まで動く）に
なるが、これは別の量どうしの比で意味を持たない。
C 実装が一時この 52 倍を「垂直 / 水平」として印字していたのは、この取り違えだった。
**「垂直荷重経路が水平経路の何倍か」を言うなら上の 31.8 倍のほう。**

### 6.3 軸方向荷重

```
F_axial = g * ( m_plate + sum_i m_i ) = 9.80665 * (15.566 + 2.150) = 173.7 N
```

軸受の摩擦トルクを後から入れるときに要るので出しておく。いまは使っていない。
`sum_i m_i = 2.150 kg` は 3.3 節の定常（液は全量）。

---

## 7. 充填

### 7.1 タイミング

停留窓の中でしか注がない。時刻は入力軸のドグを近接センサで拾った 1 回転 1 パルスからの経過で
決める（`control.cam_home_sensor`）。停留の開始を `t_d`（`psi = pi` の瞬間）として、

| 事象 | 時刻 | 出どころ |
|---|---|---|
| 開指令 | `t_d + start_delay` = `t_d + 0.050` | `fill.start_delay_s` |
| 流出開始 | `+ valve_open_delay` = `t_d + 0.080` | `fill.valve_open_delay_s` |
| **閉指令**（`V >= V_cmd` になった瞬間） | `t_d + 1.262` | 下記の先読み |
| 閉じ切り＝流出終了 | `+ valve_close_delay` = `t_d + 1.292` | `fill.valve_close_delay_s` |
| 停留終了 | `t_d + 1.500` | 残り 0.208 s |

**取り決め（`params.json` が閉じ命令の出し方を決めていないので、ここで決めた）**:
**公称の閉じ遅れだけを先読みして早めに命じる。**

```
V_cmd = target_volume - flow_rate * valve_close_delay = 400.0 - 330*0.030 = 390.1 mL
```

こうすると正常時はちょうど 400.0 mL で止まる。**故障ぶんの遅れは先読みしない**
（制御側は弁が劣化したことを知らないため）。だから閉じ遅れ故障が過充填として出る（8.6 節）。

時間予算の数え方が 2 通りあるので、どちらか明示すること。

| 数え方 | 充填にかかる時間 | 停留 1.5 s に対する余裕 |
|---|---:|---:|
| 閉じ遅れを直列に積む（`params.json` の `_timing_note` 流儀・**保守側**） | 1.322 s | 0.178 s |
| 実装（閉じ遅れを先読みするので流出終了が閉じ切り） | 1.292 s | **0.208 s** |

`params.json` の `_timing_note` の余裕 0.228 s は `start_delay` を数えていない。
**保守側で見るなら 0.178 s、実装の実際は 0.208 s。**

**PLC の 5 ms 格子は入れない。** `control.plc_scan_ms = 5.0` はあるが、実装は指令時刻を
丸めていない。理由は 2 つ。

- 格子の位相を `t = 0` に固定すると、**公称の開指令 `t_d + 0.050` はもともと格子点に
  乗る**（`1.550 s` は 5 ms の倍数）。狙っていた揺れ（`M_bend` が 600 点中 1 点だけ
  0.05 N m 外れる、`core/README.md` の `make steady`）は**消えない**。あれは開弁時刻が
  積分刻みの格子とちょうど重なることから来る境目の丸めで、指令の量子化とは別物。
- 入るのは閉じ側の量子化だけで、最大 `330 mL/s * 5 ms = 1.65 mL`（0.41 %）。
  これは弁の遅れモデルと区別がつかない差を足すだけになる。

**入れるとしたらセンサ層の取り決めとして別に置く。** 指令の刻みを主張に使うなら、
「PLC スキャンで 1.65 mL 量子化される」という話を独立に立てること。

**停留窓を出たら弁は必ず閉じる。** 実装は充填を「停留中」の条件の内側でしか進めないので、
割出しに入った時点で流出が止まる。目標量に達していなくてもそこで止まる。
前版は目標量に達するまで開き続けたので、タクトを詰めるとノズルの下にいないホルダに
注ぎ続けていた。**不足量は専用の列としては持たない**（`V_i` の到達値を見る）。

### 7.2 液量と液深

流出中は

```
V' = flow_rate = 330 mL/s = 3.30e-4 m^3/s
h  = V / (pi*R^2)                      # 円筒部にある間はこれでよい
```

液面上昇は `Q/(pi*R^2) = 99.4 mm/s`。1.212 s で 120.5 mm 上がる。

### 7.3 噴流

ノズル内径 14 mm、噴流速度 `Q/A = 2.1437 m/s`。鉛直下向きの運動量流束は

```
F_jet = rho * Q * v = 1000 * 3.30e-4 * 2.1437 = 0.7074 N
```

満量の液の重さ 3.9227 N の 18 %。**水平合力には出ないが、`Rp` に載るので支持部の曲げには
出る**（6.1 節）。開弁・閉弁でステップ状に入るので、タクトに同期した段差になる。

`M_jet = Rp * F_jet * t_hat_(充填ステーション)`（流出中のみ）。

**噴流が液面を叩いて起こす揺れは扱わない。** ノズルの偏心量が `params.json` に無く、
自由表面の非線形（波の砕け・泡・巻き込み）も等価振り子では表せない。11 節に書く。

### 7.4 質量が増えることの効果

液はノズルから来る。ノズルは世界座標に固定なので、入ってくる液の接線速度は 0。
テーブル軸まわりの角運動量の収支は

```
d(J_load*om)/dt = T_table + 0      ->      J_load*al = T_table - (dJ_load/dt)*om
```

`J_load` は `m0` だけを数える側（3.3 節）なので、増えるのも `m0` の分だけ。
**解析微分で書く。数値差分は取らない。**

```
m1     = rho * 2*R*A*tanh(eps1*h/R) / (eps1*(eps1^2-1))        （h = V/A なので V が消える）
dm1/dV = 2*rho / ( (eps1^2 - 1) * cosh^2(eps1*h/R) )
dJ_load/dt = Rp^2 * ( rho - dm1/dV ) * Q
```

| 液深 | `dJ_load/dt` [kg m^2/s] |
|---|---:|
| `V = 0`（注ぎ始め） | 0.002726 |
| `V = 400 mL`（満量） | 0.016706 |

満量では `dm1/dV` が `rho` の 1e-5 倍まで落ちるので、`Rp^2*rho*Q = 0.016706` とほぼ同じ。
浅いところでは揺れる分が増えるほうに食われるので 1/6 になる。

**公称条件では停留中に充填が終わるので `om = 0`、この項は厳密に 0**（丸めですら
0 にならない量ではない）。それでも式として入れる。タクトを詰めると停留に収まらなくなり、
そこで効き始めるため。「条件によって無害」であることと「式に無い」ことは違う。

鉛直方向の運動量は 7.3 節の `F_jet` がそのまま担当する。

---

## 8. 受け渡しと故障モード

### 8.1 供給と排出は 2 ステーション離す

供給 315 deg（ホルダ番号 `7-(k+1) mod 8`）、排出 **225 deg**（`5-(k+1) mod 8`）。
**同じポケットで同じ瞬間に抜いて入れるのは幾何的に不可能**なので、同一角には置かない。
さらに、**隣り合う 1 ステーション（45 deg）でも成立しない。** 星車の寸法で決まる。

#### なぜ 270 deg から 225 deg へ動いたか

星車（スターホイール）のポケットピッチを、テーブルのステーション間隔に合わせる。

```
ステーション間隔 = pi * 0.450 / 8 = 176.71 mm          # ピッチ円周 / 8
星車 4 ポケットでこの間隔を作る -> 星車のピッチ半径 r_s = 176.71 * 4 / (2*pi) = 112.5 mm
星車の中心 = テーブル軸から Rp + r_s = 225 + 112.5 = 337.5 mm
```

**ポケットピッチを合わせた結果、星車が大きくなる。** 供給用と排出用の 2 つの星車を
テーブルまわりに角度 `th` だけ離して置くと、2 つの星車中心の距離と、
それぞれが抱えるボトルどうしの隙間は

```
中心間距離 d(th) = 2 * 337.5 * sin(th/2)
ピッチ円の隙間   = d(th) - 2*r_s = d(th) - 225 [mm]
```

| `th` | 中心間距離 | ピッチ円の隙間 | 判定（ボトル外径 68 mm） |
|---:|---:|---:|---|
| 45 deg（1 ステーション） | 258.3 mm | **33.3 mm** | **当たる**（68 mm が入らない） |
| 53.3 deg | 303.0 mm | 78.0 mm | ぎりぎり成立（逃げ 10 mm 込み） |
| 90 deg（2 ステーション） | 477.3 mm | 252.3 mm | 成立 |

**隙間 33.3 mm に対してボトル外径は 68 mm**（内径 65 + 肉厚 1.5 × 2）なので、
1 ステーションでは両方の星車が抱えるボトルどうしが当たる。
逃げ 10 mm を見ると `2*112.5 + 68 + 10 = 303 mm` 以上の中心間距離が要り、
角度にして **53.3 deg 以上**。ステーション間隔が 45 deg なので、**2 ステーション（90 deg）が
最小**。だから排出は供給の 2 つ手前、315 - 90 = **225 deg** に置く。

**実機で供給と排出が離れているのはこの理由。** 工程角は「空いているから決める」ものではなく、
受け渡し機構の寸法から決まる。この変更で滞留が 7 割出しから 6 割出し（18.0 s）に減り、
定常のテーブル上も 満量 5 + 空瓶 1 + 空ホルダ 2 に変わった（3.3 節）。

**星車そのものはモデルに入れていない。** 入っているのはこの幾何から決まった工程角だけで、
星車の回転・受け渡しの速度・当たりの力は扱わない（8.2 節・11 節）。

#### 受け渡しで何が起きるか

どちらも停留中に起きる。時刻は停留開始から一定の遅れ `t_transfer` を置く。
**この遅れは `params.json` に無いので、当面は `t_d`（停留開始）と同時とする**（11 節）。
**停留の先頭の 1 ステップで一瞬に起きる**ものとして実装してある。

排出でリセットするもの、しないもの:

| 量 | 排出で | 供給で | なぜ |
|---|---|---|---|
| `V_i` / `h_i` | 0 | 0 のまま | 中身ごと抜く / 入るのは空瓶 |
| `phi_t_i` / `phi_r_i` と角速度 | 0 | 0 のまま | 揺れているのは抜かれた液 |
| 水平反力への寄与 | 0 | 0 のまま | `V = 0` のホルダは空瓶の慣性反力も数えていない（2.4 節） |
| `present_i`（在荷フラグ） | 1 -> 0 | 0 -> 1 | 次の停留で供給を受けるまでボトルが無い |
| `spill_i` | **そのまま** | そのまま | ホルダ単位の通算として積算を続ける |
| 適用範囲のラッチ | そのまま | そのまま | 一度外れた記録を消さない |

供給では空ボトル（0.025 kg、液なし、揺れなし）を置く。
**欠品（8.3 節）のときは供給が空振りし、`present_i` は 0 のまま。**

`j_load`・`m_bend_*`・`f_tab_*` は、この 2 つの事象でタクトに同期した段差を持つ。
**異常ではなく機械の動作**なので、正常データにも同じものが入る。
微分を取る処理には必ずこの段差が乗る。

### 8.2 受け渡しの衝撃はイベント列で出す

ボトルがポケットに入る／出るときの当たりは、**連続量に乗せない**。
リンギングの周波数が 3 kHz 級で、連続ログ 4 kHz ではナイキストを割って折り返すため
（`sim._log_rate_note`、`SENSORS.md` 5.5 節）。

1 件は `{時刻, 種別, ホルダ番号, テーブル角, モータ軸角, 種別ごとの値 d0..d7}`。
**バイト並びは `core/FORMAT.md` 5 節が正典**（`RFEVT002`）。
波形への合成はセンサ層が各センサの刻みで行う。

| `kind` | 事象 | `d0..` に載るもの |
|---:|---|---|
| 0 | 軸受外輪傷（モータ軸） | 加速度 / リンギング周波数 / 減衰比 |
| 1 | カムフォロワの当たり | 衝撃トルク / すきま |
| 2 | 供給の受け渡し | 空瓶の質量 |
| 3 | 供給が空振り（欠品でボトルが載らなかった） | 無し |
| 4 | 排出の受け渡し | 体積 / 合成傾き / `phi_t` / `phi_r` / `dphi_t` / `dphi_r` / 質量 |
| 5 | 弁の液垂れ | 液垂れ体積 |

**種別 2・4 の当たりの振幅は `params.json` に無い**（11 節）。
振幅・リンギング周波数・減衰比のどれも無いので、**出せるのは時刻だけ。**
値が入るまでは振幅を入れない。時刻が出ていれば、あとから振幅を掛けるのはセンサ層でできる。

**排出されたボトルの液は、揺れたまま外界へ持ち出される。** そのときの体積・傾き・
振り子角・角速度は種別 4 のイベントに残す。連続量には残らない。

### 8.3 欠品（`faults.missing_bottle`）

供給スターホイールの取りこぼしで、指定ホルダだけボトルが載らないまま回る。
単ノズルなので特定ステーションだけ充填量が減ることは起きない。**ボトルが無ければ充填が飛ぶ。**

- 欠けるのは液 0.400 kg（瓶を含めば 0.425 kg）。前版の充填不足 0.120 kg の 3.3 倍。
- ホルダに固定されるので、テーブル角に同期する。次数 1 の解析がそのまま使える。
- 6.1 節の曲げに `0.425*9.80665*0.225 = 0.93776 N m` として直接乗る。
- **供給が空振りしたことはイベント（種別 3）で出す。** 連続量では `present_i = 0` が
  そのホルダに残る。`V_i = 0` だけでは空瓶と区別できないので、この列で見ること（10.2 節）。
- 指定ホルダは毎サイクル空振りするので、そのホルダは通しで空のまま回る。

### 8.4 カムフォロワ摩耗（`faults.cam_follower_wear`）

外輪が摩耗して予圧が抜け、割出しの入り口と出口でカムリブとの間に微小な当たりが出る。
バックラッシュ（4.4 節）の代わりに置いたもの。

- **テーブル角の `±clearance_deg/2` オフセットは入れない**（`clearance_deg = 0.05`）。
  テーブル角はカムから運動学で与えていて積分していないので、押している側が入れ替わる
  瞬間に速度を伴わない角度の跳びが入ることになる。まともに扱うには**接触剛性**が要るが、
  `params.json` に無い。**当たりはイベントでだけ出す。**
- 入り口（`psi = 0`）と出口（`psi = pi`）で、`impact_torque_Nm = 0.2` の衝撃イベント
  （種別 1）を出す。1 サイクル 2 件。
- したがって**連続量はカムフォロワ摩耗の有無で 1 ビットも変わらない。**
  この不変性を逆にチェックとして使っている（`core/check_events.py`）。
- **割出しの途中（`tau = 0.5`、加速度が符号を変えるところ）でも当たりが起きるはず**だが、
  `params.json` の記述は入り口と出口だけなので、そこに合わせる。未確認（11 節）。

### 8.5 軸受外輪傷（`faults.bearing_outer_race`）

**テーブル軸ではなくモータ軸**に置く。テーブルは 2.5 rpm で規格の適用範囲の外だが、
モータは 1500 rpm で回り続ける。

```
モータ軸角  th_m = gear_ratio * psi = 75 * psi         （入力軸に直結）
回転周波数  25 Hz  ,  外輪傷 25 * 3.58 = 89.5 Hz
```

`th_m` が `2*pi/defect_freq_ratio` 進むごとに 1 回、イベントを出す。
**時間軸で等間隔になるのは入力軸が一定速だからで、これは前版と違う**
（前版はテーブル軸に置いていたので停留中は打たなかった）。
入力軸は連続回転なので、**停留中も打つ**。ここは間違えやすい。

1 タクトあたりの発生回数は `89.5 * 3.0 = 268.5 回`。前版の 0.44 回から 600 倍で、
包絡線解析が実用域に入る。

### 8.6 弁の閉じ遅れ（`faults.valve_close_delay`）

液固着やパッキン劣化で充填弁の閉じが遅れる。**全ボトルが等しく過充填になる。**

- 閉じ命令は 7.1 節のとおり**公称の遅れ 30 ms だけ**を先読みして出す。
  制御側は弁が劣化したことを知らないので、故障ぶんの `extra_delay_s = 0.15 s` は
  先読みできない。
- したがって充填量は `V_cmd + flow*(0.030 + 0.150) = 390.1 + 59.4 = 449.5 mL`。
  **公称 400.0 mL に対して +49.5 mL。**
- 液垂れ `drip_volume_mL = 3.0` を、弁が閉じ切った時刻にイベント（種別 5）で出す。
  落ちた先の扱い（割出し中のテーブル上に落ちる）はモデルに入れていない。
- **先読みを故障ぶんまで伸ばすと故障が消える。** 前版はそうなっていて、
  閉じ遅れを入れても 400 mL のままだった。ここは実装の分かれ目なので明記する。

---

## 9. 積分

固定ステップ。**半陰的オイラー（速度を先に更新してから位置を更新）を既定とする。**

```
v <- v + a(x, v, t)*dt
x <- x + v*dt
```

`dt` は `sim.dt_s = 2.5e-5 s`。積分するのは 2 節の振り子だけ（ステーションごとに 4 状態）。
テーブル角は 4.3 節の式で直接与える（積分しない）。
結合項（コリオリ・オイラー・遠心）は**そのステップの速度を使う**（陽的）。

`dt` の根拠:

- スロッシング周期 0.26657 s に対して 10663 分割。十分。
- `log_rate_hz = 4000` に対して `1/(4000*2.5e-5) = 10` ステップちょうど。割り切れる。
  `2.0e-5` だと 12.5 になって 13 に丸まり、実効ログが 3846 Hz になって全スペクトルが 4 % ずれる。
  （`params.json` の `_dt_note` は「20 分割」と書いているが、正しくは 10 分割。）

起動時に、スロッシング周期に対する分割数を確認して、粗すぎるなら警告を出す。

---

## 10. 出力

`log_rate_hz = 4000` に間引いて、float32 のバイナリで吐く。版は **`RFILL004`**。
**バイト並びの取り決めは `core/FORMAT.md` が正典**で、この節はそこに何を載せるかだけを決める。
列の並びは `py/ref.py` の `Record` と 1 対 1。

### 10.1 スカラ（レコード先頭・15 個）

`core/FORMAT.md` 2.1 節の並びそのもの。**順番を変えないこと。**

| # | 名前 | 単位 | 内容 |
|---:|---|---|---|
| 0 | `t` | s | 時刻 |
| 1 | `psi` | rad | **カム入力軸角。`[0, 2*pi)` に畳んである**（単調増加ではない） |
| 2 | `th_t` | rad | テーブル角。単調増加 |
| 3 | `omega` | rad/s | テーブル角速度。停留中は厳密に 0 |
| 4 | `alpha` | rad/s^2 | テーブル角加速度。停留中は厳密に 0 |
| 5 | `th_m` | rad | **モータ軸角** = `motor_speed * t`。単調増加。軸受故障の置き場 |
| 6 | `j_load` | kg m^2 | テーブル側の負荷慣性。**液は `m0` だけ**（3.3 節） |
| 7 | `torque_table` | N m | テーブル軸トルク（4.5 節）。`torque_slosh` は既に**引いて**ある |
| 8 | `torque_input` | N m | カム入力軸トルク（4.7 節）。**符号付き** |
| 9 | `torque_slosh` | N m | 揺れる液が**テーブルに及ぼす**トルク（2.4 節） |
| 10 | `m_bend` | N m | 曲げモーメントの大きさ = `hypot(m_bend_x, m_bend_y)`（6 節） |
| 11 | `m_bend_x` | N m | 同 世界座標 x 成分 |
| 12 | `m_bend_y` | N m | 同 世界座標 y 成分 |
| 13 | **`f_tab_x`** | N | テーブルが受ける水平合力の**世界座標 x 成分**（2.4 節） |
| 14 | **`f_tab_y`** | N | 同 **y 成分** |

**列名を `f_tab_t` / `f_tab_r` から `f_tab_x` / `f_tab_y` に直した。**
以前は接線・半径の名前が付いていたが、**中身は世界座標**だった（しかも順番が y, x）。
名前を中身に合わせ、並びを x, y に揃え、符号も反作用側に統一した（2.4 節）。
**旧版のデータと取り違えると静かに間違う。** 版は magic（`RFILL004`）で分ける。

回転基底へ分解しないのは、前版の「世界角 0 に固定したセンサ方位で分解」が
センサの取り付けを決め打ちしていたため。取り付けは未決なので、世界座標のまま出して
方位への分解はセンサ層に任せる。

**消えた列**: `th_cmd`（指令角。指令が無い）、`i_q` / `motor_current`（電流。4.7 節）、
`motor_torque`（`torque_input/75` で作れる）、`T_bl` / `bl_slip`（バックラッシュ）、
`a_bear`（衝撃はイベント列へ）。
**足した列**: `psi`, `th_m`, `torque_input`, `m_bend` / `m_bend_x` / `m_bend_y`。

### 10.2 ステーションごと（5 個 × ステーション数）＋ 在荷フラグ

| # | 名前 | 単位 | 内容 |
|---:|---|---|---|
| +0 | `V_i` | m^3 | 液量。**mL にするには 1e6 倍** |
| +1 | `h_i` | m | 液深 = `V_i/(pi*R^2)` |
| +2 | `phi_t_i` | rad | 振り子角（接線）。正 = 回転の進む向きに液面が高い |
| +3 | `phi_r_i` | rad | 振り子角（半径）。正 = 外側が高い |
| +4 | `spill_i` | m^3 | こぼれた量の積算（受け渡しでもリセットしない） |

**レコード末尾に在荷フラグ `present`（u8 × ステーション数）を足した。**

| 値 | 意味 |
|---:|---|
| 1 | ホルダにボトルが載っている（**液が 0 の空瓶も 1**） |
| 0 | ホルダにボトルが無い（排出済みで、まだ供給に来ていない） |

**`V_i = 0` だけでは「空瓶が載っている」と「ボトルが無い（欠品を含む）」を区別できない。**
質量も慣性も曲げも変わるのに、区別する手段が無かったので列にした。
既定条件の定常では 1 が 6 個、0 が 2 個（3.3 節）。

**持たない列**: `F_react_t_i` / `F_react_r_i`（合成した `f_tab_x` / `f_tab_y` だけを出す）、
`dz_over_R_i`（読む側で `tan(hypot(phi_t, phi_r))` として作る）、
`range_flag_i`（ヘッダの `range_exceeded` / `max_dz_over_R` に集約）。

### 10.3 イベント列

8.2 節の 6 種別。**連続量とは別ファイル**（`RFEVT002`、`--out` の名前 + `.events`）。
連続ログには衝撃系の列を**一切置いていない**。イベント列が唯一の出どころ。

---

## 11. 書かない・主張しない

このラボは記事の裏づけであって、機械の設計書ではない。

### 11.1 前提

- **実機の実測ではない。合わせ込みもしていない。**
- 主張してよいのは「**この物理とこの諸元の下では**、こうなる」まで。
- 「実機でも同じ数字になる」とは書かない。
- 数値の桁が合っていることと、値が正しいことは違う。

### 11.2 これは主張できない（実装から分かった範囲。名指しで挙げる）

記事で言ってよいことの境目。**ここに挙げたものは、根拠が無いか、根拠が仮置きの上に
乗っているので、主張の柱にしない。**

| 言えないこと | なぜ |
|---|---|
| **入力軸トルクの値** | 摩擦 3 点（効率 0.85 / 引きずり 0.30 N m / 粘性 0.05 N m s/rad）が**出所無し**。**入力軸ピークトルク 0.71548 N m の 42 % を引きずりが占める。** 3 つを振ると 3 〜 4 割動く |
| **モータ電流** | トルク電流係数と無負荷電流が `params.json` に無い。**列としても出していない。** 旧モデルでは主要な観測量だったが、いまは根拠が無い |
| **定速区間のトルクが `c_visc*om` に一致すること** | 旋回軸受の摩擦トルクが `params.json` に無く 0 にしている。**厳密に粘性だけになるのはモデルの都合**で、実機ではこうならない |
| **受け渡し衝撃の大きさ・波形** | 振幅・リンギング周波数・減衰比のどれも無い。**出せるのは時刻だけ**（8.2 節の種別 2・4） |
| **空瓶ホルダの水平反力** | `V = 0` のホルダは空瓶の慣性反力（0.011 N）も数えていない。実装が `V <= 0` を飛ばしているのに合わせた |
| **噴流の力の値** | ノズル出口から液面までの落差が無い。既定 0 で 0.7074 N だが、落差 50 mm で 0.79 N（**+12 %**）。**12 % 動きうる量として扱う** |
| **この機構が成立すること** | 素ユニットの許容負荷慣性が未確認（`allowable_inertia_kg_m2: null`）。割付を 270 deg から 180 deg にすると許容値は 0.44 倍に落ちる。**機構として成立するかが確かめられていない** |
| **スワーリング（液が回りながら揺れる）** | 2 方向の等価振り子では**原理的に出ない**。回転テーブルが題材なので必ず断る |
| **液深による固有周波数の変化** | `h/R` が大きいと `tanh` が飽和する。液深 50 mm で最終値の 99.7 %。**深いところではほとんど動かない。** 効くのは注ぎ始めだけで、誇張しない |
| **こぼれた量 [mL]** | 柱近似のままで 1〜2 桁の過大評価（2.6 節）。**上限値としてしか出せない** |
| **垂直 / 水平の比 31.8 倍** | 腕 `L_arm = 0.150 m` がセンサ側からの流用で、**機械の重心高さとして未確認**。桁の話まで |
| **転倒余裕 12 倍** | ボトルの重心高さが `params.json` に無いので、しきい値 0.532 g 自体を追いかけられていない |

### 11.3 仮置きのまま残っている値（名指しで挙げる）

| 値 | 状態 | 効き方 |
|---|---|---|
| `indexer.efficiency = 0.85` | 出所なし | `T_cam` を 18 % 膨らませる（減速側では 15 % 減らす） |
| `indexer.input_drag_torque_Nm = 0.30` | 出所なし | **入力軸ピーク 0.71548 の 42 %** |
| `indexer.table_viscous_Nms_rad = 0.05` | 出所なし | テーブル軸トルクの 1 % 未満（ピークの瞬間で 0.01147 / 1.42004） |
| `drive.motor_rated_output_W = 40` | 負荷計算から決まっていない。実負荷は定格の 3.5 % | 選定の話。物理には効かない |
| `indexer.allowable_inertia_kg_m2 = null` | **未確認**。割付を 270 deg から 180 deg にすると許容値は 0.44 倍に落ちる | **成立するかどうかが未確認** |
| `liquid.slosh_damping_ratio = 0.005` | 相関式に対して安全側に丸めた値 | 共振の鋭さを決める。傾きに直に効く |
| `sensors.strain.arm_length_mm = 150` | センサ側の値。**機械の重心高さとして未確認** | 6.2 節の 31.8 倍がこれに反比例する |
| 停留開始から受け渡しまでの遅れ | `params.json` に無い | 停留開始と同時にしている |
| センサの取り付け位置と荷重経路 | 未決。`sensors` ブロックは前版の持ち越し | 断面も方位も暫定 |

**入力軸トルクとモータ電流を主張の根拠にしない。** 上の 3 つの摩擦だけで 3 割から 4 割動く。
**テーブル軸トルクのほうは摩擦にほとんど依らない**ので、こちらは使える。

### 11.4 モデルに入っていないもの

- **スワーリング**（液が回りながら揺れる）。2 方向の等価振り子では原理的に出ない。
  回転テーブルが題材なので、この制限は必ず断る。
- **自由表面の非線形**。波の砕け、泡、空気の巻き込み。線形の 1 次モードだけを見ている。
- **注入ジェットによる励振**。ノズルの偏心量が無く、噴流が液面を叩く現象も等価振り子の外。
  鉛直の運動量（7.3 節）だけは入れてあるが、水平の励振は入れていない。
  **噴流の運動量流束は満量の液の重さの 18 %** あるので、実機では割出しより強い揺れの源に
  なり得る。「揺れを起こしているのは割出しだけ」とは書かない。
- **駆動系のねじり弾性**（4.6 節）。ねじり共振の線は出ない。
- **旋回軸受の摩擦**。定速区間のトルクが厳密に粘性だけになるのは、この欠落のため。
- **入力軸速度の変動**。すべりで回るモータを一定速として扱っている（11.6 節）。
- **架台の 3 次元的な変形**。支持部を 1 点として扱っている。
- **水平反力による曲げ `M_horiz`**（6.1 節）。腕の長さが未確認なので列に入れていない。
- **カムフォロワ摩耗のテーブル角オフセット**（8.4 節）。接触剛性が無いので入れていない。
  したがって連続量はこの故障の有無で変わらない。
- **PLC の 5 ms 格子**（7.1 節）。入れても狙った揺れは消えず、閉じ側に 1.65 mL の
  量子化が増えるだけなので入れていない。
- **星車そのもの**（8.1 節）。工程角を決めるのに幾何を使っただけで、
  受け渡しの速度も力もモデルに無い。
- **ボトルの転倒・ホルダ内での滑り**。接線加速度は転倒しきい値の 1/12 だが、
  重心高さが `params.json` に無いので、その 1/12 自体が追いかけられていない。

### 11.5 線形近似の有効範囲を超えたら何が言えなくなるか

`dz/R <= 0.2` を超えると、次が全部言えなくなる。

- **傾きの値そのもの**。減衰が振幅に依存し始めるので、`zeta` 一定の前提が崩れる。
- **固有振動数**。振幅とともに下がる（非線形の軟化）。
  スペクトルのピーク位置を「液深から決まる `w1`」として読むことができなくなる。
- **反力とトルク**。`m1`/`m0` の分割は線形モードの理論から出ているので、同時に無効になる。
- **こぼれ量**。柱で落とす近似（2.6 節）は液面が平面である前提。

したがって範囲を外れた区間は、**外れたという事実だけを出力し、値は使わない。**
`dz/R` の最大はヘッダの `max_dz_over_R` / `range_exceeded` としてラッチして残す。
公称条件では最大 0.09422（2.6 節）なので内側に収まるが、タクトや割出し時間を振ると外れる。
**どこで外れるかを見るのが解析の主題なので、外れること自体は目的のうち。**

### 11.6 `params.json` の中で辻褄が合っていないところ

実装はどちらかを選ばざるを得ないので、選んだ側と理由を書く。

| 箇所 | 何が合わないか | この文書での扱い |
|---|---|---|
| `table._inertia_note` の「7 本満量＋1 本空」 | `stations` の工程配置からは **満量 5 ＋空瓶 1 ＋空ホルダ 2**。`J_load` で 6.0 % 差 | 3.3 節。実装は毎ステップ計算。0.7621 は上限として選定にだけ使う |
| `drive.inverter_hz_at_operating_point` | 4 極・定格 1420 rpm から出るすべりは定格トルクでの値。実負荷は定格の 3.5 % なので同期速度の近くで回る。1500 rpm を得るには約 50.1 Hz（rev.3 で修正済み） | 4.1 節。**入力軸は 20 rpm 一定と規定**し、すべりの動特性は扱わない |
| `cycle._kinematics_note` の「余裕」 | 充填の余裕 0.228 s は `start_delay` を数えていない | 7.1 節。保守側で 0.178 s、実装の実際は 0.208 s |
| `cycle` の `table_alpha_max` / `tangential_accel_max` | 丸めた `Ca = 5.528` から出した値。閉じた形なら 1.92962 / 0.43416 | 4.3 節。**実装の値を採る**（相対差 1e-5） |
| `sim._dt_note` の「20 分割」 | `1/(4000*2.5e-5) = 10` | 9 節。10 分割 |
| `indexer.source` の「入力軸ピーク負荷トルク 0.76 N m」 | トルクの最大と速度の最大が同時に起きない（1.78 倍の過大） | 4.7 節。実際は 0.40290 N m（慣性負荷だけ） |
| `viz.cameras[0]` の説明文 | 「供給 315 / 充填 0 / **排出 270**」のまま | 1.2 節。排出は 225 deg |

### 11.7 `params.json` に足りない値

無いと式が書けない、または仮置きで逃げているもの。

| 要る値 | 何に使うか | いまどうしているか |
|---|---|---|
| モータのトルク電流係数と無負荷電流 | インバータ出力電流（4.7 節） | **電流を出さない。`T_in` までで止める** |
| 減速機の効率 | `T_m`（4.7 節） | 1.0 として扱う |
| 旋回軸受の摩擦トルク | `T_table`（4.5 節） | 0 として扱う。**定速区間が厳密に `c_visc*om` になるのはこの欠落のため** |
| 受け渡し衝撃の振幅・リンギング周波数・減衰比 | イベント列の種別 2・4（8.2 節） | 時刻だけ出す。振幅は入れない |
| 停留開始から受け渡しまでの遅れ | 8.1 節 | 停留開始と同時 |
| ボトルの重心高さ | 転倒しきい値（4.3 節）、`L_arm` の根拠（6.1 節） | センサ側の 150 mm を流用。**未確認** |
| テーブル板から支持部までの距離 | 曲げモーメントの腕（6.1 節） | 同上。`M_horiz` を入れていない |
| ノズル出口から液面までの落差 | 噴流の着液速度（7.3 節） | 0。50 mm 入れると力が **12 % 増える** |
| ノズルの偏心量 | 噴流の水平励振（7.3 節） | 扱わない |
| カムフォロワの接触剛性 | 摩耗のテーブル角オフセット（8.4 節） | 入れない。当たりはイベントだけ |
| `indexer.allowable_inertia_kg_m2` | 機構が成立するかの判定 | `null` のまま。**未確認** |

`params.json` の `physics_todo` ブロックは 2・6・7.4・8 節が実装された時点で役目を終える。
**4 項目（`vertical_load_path` / `coriolis` / `variable_mass_momentum` /
`star_wheel_transfer_impact`）はいずれも実装に入った**（星車の衝撃は時刻だけ）。
消してよい。
