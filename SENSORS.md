# センサと故障モードの取り決め

このラボの主題は「実機が無い段階で、センサ信号を物理から作り、異常監視の仕組みを組んで検証できるか」。
`MODEL.md` が機械の物理、この文書がその上に載るセンサと故障の取り決め。

パラメータは `params.json` の `sensors` / `faults` を読む。数値を直書きしない。

## 1. まず物理側に必要な追加（これが無いとセンサが何も拾えない）

いまのモデルは、テーブルが液を揺らす一方向の結合しかない。
**揺れた液がテーブルに返す反力**を入れる。これを入れないと、加速度センサもひずみゲージも
スロッシングを検出できず、記事が成り立たない。

等価振り子で、液の質量 `m_liq` を二つに分ける。

```
m1 = m_liq * (R / (2.2 * h)) * tanh(1.8412 * h / R)      # 揺れる分
m0 = m_liq - m1                                          # 容器と一緒に動く分
```

揺れる質量の水平絶対加速度（方向ごと、小角近似）:

```
a1 = a_tank + L1 * phi''
```

液がボトルに及ぼす水平力（方向ごと）:

```
F_liq = m0 * a_tank + m1 * a1
```

ステーション i の接線方向の力が、テーブル軸に返すトルク:

```
T_slosh = - sum_i( F_liq_tangential_i * Rp )
```

これを割出し軸の運動方程式に足す（双方向結合になる）。
半径方向の力は軸トルクには効かないが、**支持軸の曲げと架台の振動には効く**ので、
合力ベクトルとして残しておく。

## 2. 仮想センサの信号連鎖

どのセンサも、真値からいきなり数字を出さない。次の順で通す。

```
真値（物理コアの状態から合成）
 → 取り付け位置の伝達（下記 3 節）
 → 帯域制限（2次バターワースの低域通過。カットオフは params の bandwidth_hz）
 → サンプリング（sim.log_rate_hz から sample_rate_hz へ間引き）
 → ノイズ加算
 → 量子化（bits と range から LSB を決める）
 → レンジ飽和
```

この連鎖を通すことが目的。**理想の物理量をそのまま出したものはセンサ信号ではない。**
量子化とノイズとサンプリングが入って初めて、検出アルゴリズムの試験になる。

ノイズは再現できるように擬似乱数の種を引数で受ける。同じ種なら同じ波形が出ること。

## 3. 各センサの真値の作り方

### 加速度センサ（架台の柱）

架台は剛体とみなし、テーブルからの反力で揺れると考える。
接線方向と半径方向の 2 軸。真値は、

```
a_frame = (テーブル反力の合力) / m_frame_equiv
```

`m_frame_equiv` は params に無いので、架台の質量を寸法から見積もって `sensors.accel` に
`frame_equiv_mass_kg` として足してよい（足したら source 欄に根拠を書く）。

構造の共振を 1 つだけ入れる。2 次系（固有振動数と減衰比を `sensors.accel` に足す）を
通してから帯域制限へ。これが無いと、軸受の衝撃が「鳴らない」ので不自然になる。

重力成分は、DC 結合なので鉛直軸には乗るが、水平 2 軸には乗らない（センサは傾かない前提）。

### ひずみゲージ（テーブル支持軸）

支持軸の根元の曲げひずみ。テーブルに掛かる水平合力 `F` と、
軸の断面係数から、

```
sigma = M / Z = (F * L_arm) / Z ,   strain = sigma / E
```

`L_arm`（力点から根元までの距離）、`Z`（断面係数）、`E`（縦弾性係数、ステンレスで 193 GPa）は
`sensors.strain` に足す。値は寸法から計算して、source に計算式を残すこと。

**ひずみゲージは加速度センサより低い周波数まで素直に見えるので、
スロッシング（3.75 Hz）を拾うのはこちらが主役になるはず。**そこを確認したい。

### モータ電流

物理コアがすでに持っている電流値に、連鎖を通すだけ。

## 4. 故障モード

`params.json` の `faults` にある 3 つ。すべて既定は `enabled: false`。
有効にしたとき、正常時との差がセンサ信号にどう出るかを見るのが目的。

### 4.1 充填量アンバランス

指定ステーションの充填量を `volume_ratio` 倍にする。
質量差がそのまま回転アンバランスになり、**回転同期（1X）の成分**として出るはず。
テーブルが等速で回っていないので、厳密な 1X ではなく回転角に同期した成分になる。
次数比分析（回転角で等間隔にリサンプリングしてから FFT）で見えるはず。

### 4.2 軸受外輪傷

テーブル軸の回転角が `1/defect_freq_ratio` 回転進むごとに 1 回、衝撃を打つ。
衝撃は瞬間的な力ではなく、`ring_freq_hz` の減衰振動として加速度に加える。

```
a_impact(t) = A * exp(-2*pi*ring_damping*ring_freq*t) * sin(2*pi*ring_freq*t)
```

振幅 `A` は `impulse_accel_m_s2`。回転速度が変わると衝撃の間隔も変わること
（時間軸で等間隔にしてはいけない。ここを間違えると次数比分析の意味が消える）。

包絡線解析（帯域通過 → 絶対値 → 低域通過 → FFT）で欠陥周波数が立つはず。
軸受監視では定石の手順なので、そのまま踏襲する。

### 4.3 バックラッシュ

モータトルクの符号が反転する瞬間、`angle_deg` ぶんの空走を挟み、
歯が当たるときに `impact_torque_Nm` の衝撃トルクを 1 ステップぶん加える。
加減速の切り替わりで、電流にも加速度にも出るはず。

## 5. 検出側に期待すること（次の段階）

いまは信号を作るところまで。検出は次。ただし信号を作る時点で、
次の 4 つが分離できる形になっているかを確認しておくこと。

- 回転同期の成分（アンバランス）
- 3.75 Hz 付近のスロッシング成分。**回転に同期しない**ので次数比分析で回転成分と分けられる
- 包絡線スペクトルの欠陥周波数（軸受）
- トルク反転時の衝撃（バックラッシュ）

とくに 2 番目は、この機械にしかない持ち味。液が減ると `w1` が動くので、
**振動から中身の状態が読める**という話にできる。ただし `h/R` が大きい領域では
`tanh` が飽和して周波数がほとんど動かないことは確認済みなので、
効くのは液が浅いところだけ。誇張しないこと。

## 5.5 改訂（衝撃はイベント列で渡す・センサは2種類）

当初は物理コアの連続ログに衝撃波形を乗せる想定だったが、これは破綻する。
`ring_freq_hz` が 3kHz なのに連続ログが 4kHz では、ナイキストを割って折り返す。
かといって連続ログを 51.2kHz にすると、データ量が現実的でなくなる。

そこで分ける。

- **連続量**（テーブル角、液面、反力、ひずみのもと、電流）は `log_rate_hz` = 4kHz の連続ログ。
- **衝撃**（軸受外輪傷、バックラッシュ）は、**発生時刻と振幅のイベント列**として別に出す。
  波形にはしない。センサ層が、各センサの刻みで減衰振動を合成して足す。

こうすると、物理は物理の刻みで、センサはセンサの刻みで扱えて、両方が正しくなる。

センサは加速度だけ 2 種類に分ける。**1 つのセンサで全部は見られない**というのが、
この記事の実用的な要点のひとつになる。

- `accel_lf`: DC 結合の MEMS、1kHz サンプリング、帯域 200Hz。
  スロッシング（3.75Hz）とアンバランスの回転同期成分を見る。
- `accel_hf`: 交流結合の圧電（IEPE）、51.2kHz サンプリング、帯域 10kHz。
  軸受の衝撃とリンギングを見る。低周波は交流結合で落ちるのでスロッシングは見えない。

**この「見えない」ことを実際に図で示すこと。** 低速センサに軸受の衝撃を通すと
折り返して偽の低周波成分になる。高速センサにスロッシングを通すと消える。
どちらも、なぜ 2 つ要るのかの説明になる。

## 6. 主張してよい範囲

- 実機の実測ではない。合わせ込みもしていない。センサの諸元も一般的な水準を置いただけ。
- 主張してよいのは「この物理とこのセンサ諸元の下では、この異常はこの特徴量に出る」まで。
- 「実機でも同じ数字になる」とは書かない。
- 2 方向独立の振り子なので、液が回りながら揺れる現象（スワーリング）は原理的に出ない。
  回転テーブルが題材なので、この制限は記事で必ず断る。
